// Function expression to divide two numbers
const divide = function(a, b) {
  return a / b;
};

// Example usage of divide function
const result = divide(2000, 100); // This will return 20

// Arrow function to square a number
const square = (x) => x * x;

// Arrow function to add two numbers
const add = (a, b) => a + b;
